spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kioskdb
    username: kiosk_user
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver  # ✅ 이거 추가!
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
