server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kioskdb
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    
